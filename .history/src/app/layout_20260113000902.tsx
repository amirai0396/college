import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "CollegeChalo - Find Your Dream College | 20,000+ Colleges",
  description: "Because We Make Your Educational Journey Simple. Discover 20,000+ colleges, 500,000+ courses and exams. Get admission guidance and compare colleges.",
  keywords: ["colleges in India", "engineering colleges", "MBA colleges", "medical colleges", "college admission", "entrance exams"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
